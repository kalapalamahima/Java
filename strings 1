/*1. Program to separate the Individual Characters from a String
import java.util.Scanner;
public class q{
  public static void main(String args[]){
    Scanner sc = new Scanner(System.in);
    System.out.println("Enter the string: ");
    String n = sc.nextLine();
    for (int i = 0; i < n.length(); i++){
      System.out.println(n.charAt(i));
    }
    sc.close();
  }
}

//17) checks the word in the sentence:
import java.util.Scanner;
public class q{
  public static void main(String args[]){
    Scanner sc = new Scanner(System.in);
    System.out.println("Enter the string: ");
    String n = sc.nextLine();
    System.out.println("Enter the word: ");
    String m = sc.nextLine();
    System.out.println(n.contains(m));
    sc.close();
  }
}

//19) String in reverse order:
import java.util.Scanner;
public class q{
  public static void main(String args[]){
    Scanner sc = new Scanner(System.in);
    System.out.println("Enter the string: ");
    String n = sc.nextLine();
    for (int i = n.length()-1; i >= 0; i--){
      System.out.println(n.charAt(i));
      
    }
    sc.close();
  }
}

// 16) replace old string with new string 
import java.util.Scanner;
public class q{
  public static void main(String args[]){
    Scanner sc = new Scanner(System.in);
    System.out.println("Enter the string: ");
    String n = sc.nextLine();
    System.out.print("Enter the old substring: ");
    String oldword = sc.nextLine();
    System.out.println("Enter the new substring: ");
    String newword = sc.nextLine();

    String result = n.replace(oldword,newword);
    System.out.println(result);
    sc.close();
  }
}
// 5) Remove character from string:
import java.util.Scanner;
public class q{
  public static void main(String args[]){
    Scanner sc = new Scanner(System.in);
    System.out.println("Enter the string: ");
    String n = sc.nextLine();
    System.out.println("Enter the character you want to delete: ");
    String m = sc.nextLine();
    String result = n.replace(m,"");
    System.out.println("String after deletion: " + result);
    sc.close();
  }
}

// program to print a string in alpha order:
import java.util.Scanner;
import java.util.Arrays;

public class q {
    public static void main(String args[]) {

        Scanner sc = new Scanner(System.in);

        System.out.print("Enter a string: ");
        String str = sc.nextLine();

        char[] ch = str.toCharArray();

        // Sort the characters
        Arrays.sort(ch);

        // Alphabetical order
        System.out.print("Alphabetical Order: ");
        for (int i = 0; i < ch.length; i++) {
            System.out.print(ch[i]);
        }

        System.out.println();

        // Reverse alphabetical order
        System.out.print("Reverse Alphabetical Order: ");
        for (int i = ch.length - 1; i >= 0; i--) {
            System.out.print(ch[i]);
        }

        sc.close();
    }
}*/ 
// import java.util.Scanner;

